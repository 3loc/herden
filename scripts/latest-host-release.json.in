{
  "version": "@VERSION@",
  "protocol": 22,
  "endpoint_generation": 1,
  "notes": "Herden Host @VERSION@ keeps a Space's shell pane open when its managed Agent exits, so the iPhone can switch that Space to its terminal and the user can resume the Agent later. It also keeps Codex's input and question panels legible on light desktop viewers.",
  "assets": {
    "linux-x86_64": "https://herden.app/releases/host-v@VERSION@/herden-linux-x86_64",
    "linux-aarch64": "https://herden.app/releases/host-v@VERSION@/herden-linux-aarch64",
    "macos-x86_64": "https://herden.app/releases/host-v@VERSION@/herden-macos-x86_64",
    "macos-aarch64": "https://herden.app/releases/host-v@VERSION@/herden-macos-aarch64"
  },
  "sha256": {
    "linux-x86_64": "@LINUX_X86_64@",
    "linux-aarch64": "@LINUX_AARCH64@",
    "macos-x86_64": "@MACOS_X86_64@",
    "macos-aarch64": "@MACOS_AARCH64@"
  },
  "releases": {
    "@VERSION@": {
      "notes": "Herden Host @VERSION@ keeps a Space's shell pane open when its managed Agent exits, so the iPhone can switch that Space to its terminal and the user can resume the Agent later. It also keeps Codex's input and question panels legible on light desktop viewers."
    }
  }
}
